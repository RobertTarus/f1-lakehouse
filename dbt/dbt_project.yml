name: "f1_lakehouse"
version: "1.0.0"

profile: "f1_lakehouse"

model-paths:
  - "models"

test-paths:
  - "tests"

clean-targets:
  - "target"
  - "dbt_packages"

models:
  f1_lakehouse:
    silver:
      +materialized: table
      +schema: silver

    gold:
      +materialized: table
      +schema: gold
